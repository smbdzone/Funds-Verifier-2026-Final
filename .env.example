
# Third-party image compression: oversized listing images are sent here and the
# compressed result is awaited before the user can proceed. Leave empty to disable.
NEXT_PUBLIC_IMAGE_COMPRESSION_API_URL=
NEXT_PUBLIC_IMAGE_COMPRESSION_API_KEY=
